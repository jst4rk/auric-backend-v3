export enum ETransactionType {
  INCOME = 'income',
  EXPENSE = 'expense',
}

export enum ECurrency {
  USD = 'USD',
  CUP = 'CUP',
}

export enum EKind {
  REGULAR = 'regular',
  EXCHANGE = 'exchange',
}
